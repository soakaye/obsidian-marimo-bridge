# Data Model: Upgrade Existing marimo Installations

There are no data model changes, database schema modifications, or lifecycle state transitions introduced by this feature. 

The feature only modifies the command arguments when executing `pip install` via the shell, depending on whether `marimo` is already installed.
