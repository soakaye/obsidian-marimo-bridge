# タスクリスト: コンテキストメニューへのノートブック作成機能の追加

**インプット**: `/specs/005-context-menu-create-notebook/` の設計ドキュメント

**前提条件**: plan.md (必須), spec.md (必須), research.md, data-model.md

**テスト**: 自動テストの記述要請はないため、手動検証シナリオによる動作確認タスクを含めます。

**組織化**: タスクは各ユーザーストーリーごとに整理されており、各ストーリーを独立して実装および検証することができます。

## 記述フォーマット: `[ID] [P?] [Story] Description`

- **[P]**: 並列実行可能（ファイルが異なる、または依存関係がない）
- **[Story]**: どのユーザーストーリーに属するか（例: US1, US2）
- タスクの説明には、具体的なファイルパスを含めます。

## パス規約

- **単一プロジェクト**: リポジトリルートの `src/`

---

## 第1フェーズ: セットアップ (共通インフラ)

**目的**: プロジェクトの初期状態確認とビルド確認

- [x] T001 プロジェクトのルートで依存関係をインストールし、既存コードで `npm run build` が通ることを確認する

---

## 第2フェーズ: 基盤 (依存関係の完了)

**目的**: ユーザーストーリーの実装を開始する前に完了しておくべきコードベースの整理

- [x] T002 `src/main.ts` において既存の `createNotebook` メソッドが定義・呼び出されている箇所を確認する

**チェックポイント**: 基盤準備完了 - 各ユーザーストーリーの実装を開始できます。

---

## 第3フェーズ: User Story 1 - フォルダのコンテキストメニューからのノートブック作成 (Priority: P1) 🎯 MVP

**ゴール**: ファイルエクスプローラ内のフォルダを右クリックして「Create new marimo notebook」を選択すると、そのフォルダ内に新規ノートブックが作成され開くこと。

**単体検証**: 任意のフォルダを右クリックして新規作成し、正しいパスに作られることを手動確認する。

### ユーザーストーリー1 の実装

- [x] T003 `src/main.ts` の `createNotebook` メソッドを拡張し、任意の `folderPath` を引数（オプション）として受け取れるように汎用化する
- [x] T004 [US1] `src/main.ts` 内に Obsidian の `file-menu` イベントを検知し、対象がフォルダの場合に「Create new marimo notebook」メニュー項目を追加するイベントハンドラーを実装する
- [x] T005 [US1] `src/main.ts` のイベントハンドラー内で、右クリックされたフォルダのパスを渡し `createNotebook` を実行するコールバックを実装する

**チェックポイント**: この時点で、User Story 1（フォルダからの作成）が単独で動作し検証可能な状態になります。

---

## 第4フェーズ: User Story 2 - ファイルのコンテキストメニューからのノートブック作成 (Priority: P2)

**ゴール**: ファイルを右クリックして「Create new marimo notebook」を選択すると、そのファイルの親フォルダ内に新規ノートブックが作成され開くこと。

**単体検証**: 任意のファイルを右クリックして新規作成し、そのファイルと同じ階層に作られることを手動確認する。

### ユーザーストーリー2 の実装

- [x] T006 [US2] `src/main.ts` の `file-menu` イベントハンドラーを修正し、対象がファイルである場合に親フォルダ `parent?.path` を作成先パスとして決定するロジックを実装する
- [x] T007 [US2] 対象ファイルが Vault ルート直下にある場合のフォールバック（ルートディレクトリへの作成）を考慮するロジックを `src/main.ts` に実装する

**チェックポイント**: この時点で、User Story 1 と 2 が両方とも動作し検証可能な状態になります。

---

## 最終フェーズ: 仕上げと横断的関心事 (Polish)

**目的**: 品質確認と動作検証

- [x] T008 `npm run build` を実行してコンパイルエラーが無いことを確認し、さらに `npm run lint` が通ることを検証する
- [ ] T009 [P] `specs/005-context-menu-create-notebook/quickstart.md` の検証手順に従い、シナリオ1〜3の手動動作確認を行う
- [ ] T010 [P] 変更内容を Git にコミットする

---

## 依存関係と実行順序

### フェーズ依存関係

- **セットアップ (第1フェーズ)**: 依存関係なし - 即座に開始可能。
- **基盤 (第2フェーズ)**: セットアップ完了に依存。
- **ユーザーストーリー (第3フェーズ以降)**: 基盤フェーズの完了に依存。
  - US1 と US2 は順次（P1 → P2）実行します。
- **仕上げ (最終フェーズ)**: すべてのユーザーストーリーの実装完了に依存。

### ユーザーストーリー依存関係

- **User Story 1 (P1)**: 基盤完了後に開始可能。他ストーリーへの依存なし。
- **User Story 2 (P2)**: 基盤完了後に開始可能。US1 の `createNotebook` 汎用化に依存。

---

## 実装戦略

### MVP優先 (User Story 1 のみ)

1. 第1フェーズ（セットアップ）を完了する。
2. 第2フェーズ（基盤）を完了する。
3. 第3フェーズ（User Story 1）を完了する。
4. **動作検証**: フォルダからの新規作成機能が単独で動作することを確認する。

### インクリメンタルデリバリー

1. セットアップ + 基盤を完了。
2. フォルダからの作成機能（US1 / MVP）を追加し、検証（デモ可能状態）。
3. ファイルからの作成機能（US2）を追加し、検証。
4. 最終ビルド・テストを行いリリース準備。
