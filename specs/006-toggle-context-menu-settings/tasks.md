# タスクリスト: 設定によるコンテキストメニュー有効/無効トグル機能の追加

**インプット**: `/specs/006-toggle-context-menu-settings/` の設計ドキュメント

**前提条件**: plan.md (必須), spec.md (必須), research.md, data-model.md

**テスト**: 自動テストの記述要請はないため、手動検証シナリオによる動作確認タスクを含めます。

**組織化**: タスクは各ユーザーストーリーごとに整理されており、各ストーリーを独立して実装および検証することができます。

## 記述フォーマット: `[ID] [P?] [Story] Description`

- **[P]**: 並列実行可能（ファイルが異なる、または依存関係がない）
- **[Story]**: どのユーザーストーリーに属するか（例: US1, US2）
- タスクの説明には、具体的なファイルパスを含めます。

## パス規約

- **単一プロジェクト**: リポジトリルートの `src/`

---

## 第1フェーズ: セットアップ (共通インフラ)

**目的**: プロジェクトの初期状態確認とビルド確認

- [x] T001 プロジェクトのルートで依存関係をインストールし、既存コードで `npm run build` が通ることを確認する

---

## 第2フェーズ: 基盤 (依存関係 of 完了)

**目的**: ユーザーストーリーの実装を開始する前に完了しておくべきコードベースの整理（設定スキーマとUIの定義）

- [x] T002 `src/settings.ts` の `MarimoBridgeSettings` インターフェースに `showContextMenu` 設定項目（boolean）を追加し、`DEFAULT_SETTINGS` に `showContextMenu: true` を追加する
- [x] T003 `src/settings.ts` 内の `MarimoBridgeSettingTab` クラスに「Enable file explorer context menu」設定トグルのUI（トグルスイッチ）を追加する

**チェックポイント**: 基盤準備完了 - 各ユーザーストーリーの実装を開始できます。

---

## 第3フェーズ: User Story 1 - 設定無効時のコンテキストメニュー非表示 (Priority: P1) 🎯 MVP

**ゴール**: 設定トグルをオフにすると、右クリックしてもメニューに「Create new marimo notebook」が表示されない。

**単体検証**: 設定トグルをオフにして、右クリックした際にメニュー項目が表示されないことを手動確認する。

### ユーザーストーリー1 の実装

- [x] T004 [US1] `src/main.ts` 内の `file-menu` イベントハンドラーのコールバック内に、`showContextMenu` 設定が無効である場合はメニュー項目を追加せず早期リターンする判定処理を実装する

**チェックポイント**: この時点で、User Story 1（メニュー非表示化）が単独で動作し検証可能な状態になります。

---

## 第4フェーズ: User Story 2 - 設定有効時のコンテキストメニュー表示 (Priority: P1)

**ゴール**: 設定トグルをオンにすると、右クリック時に「Create new marimo notebook」が表示され機能する。

**単体検証**: 設定トグルをオンにして、右クリックした際にメニュー項目が表示され、クリックで正しく作成されることを手動確認する。

### ユーザーストーリー2 の実装

- [x] T005 [US2] `src/main.ts` 内の `file-menu` イベントハンドラーで設定が有効な場合にのみメニュー項目が作成・バインドされるように全体の条件分岐を検証・調整する

**チェックポイント**: この時点で、User Story 1 と 2 が両方とも動作し検証可能な状態になります。

---

## 最終フェーズ: 仕上げと横断的関心事 (Polish)

**目的**: 品質確認と動作検証

- [x] T006 `npm run build` を実行してコンパイルエラーが無いことを確認し、さらに `npm run lint` が通ることを検証する
- [ ] T007 [P] `specs/006-toggle-context-menu-settings/quickstart.md` の検証手順に従い、シナリオ1〜2の手動動作確認を行う
- [ ] T008 [P] 変更内容を Git にコミットする

---

## 依存関係と実行順序

### フェーズ依存関係

- **セットアップ (第1フェーズ)**: 依存関係なし - 即座に開始可能。
- **基盤 (第2フェーズ)**: セットアップ完了に依存。
- **ユーザーストーリー (第3フェーズ以降)**: 基盤フェーズ（設定の定義）の完了に依存。
  - US1 と US2 は並行または順次（P1 → P1）実行します。
- **仕上げ (最終フェーズ)**: すべてのユーザーストーリーの実装完了に依存。

### ユーザーストーリー依存関係

- **User Story 1 (P1)**: 基盤完了後に開始可能。
- **User Story 2 (P1)**: 基盤完了後に開始可能。

---

## 実装戦略

### MVP優先 (User Story 1 のみ)

1. 第1フェーズ（セットアップ）を完了する。
2. 第2フェーズ（基盤）を完了する。
3. 第3フェーズ（User Story 1）を完了する。
4. **動作検証**: 設定をオフにした際に右クリックメニューが出ないことを確認する。

### インクリメンタルデリバリー

1. セットアップ + 基盤を完了。
2. メニュー非表示化（US1 / MVP）を追加し、検証。
3. メニュー表示・動作（US2）を追加し、検証。
4. 最終ビルド・テストを行いリリース準備。
