# タスクリスト: リボンボタンのマウス操作変更機能の追加

**インプット**: `/specs/007-ribbon-click-behavior-change/` の設計ドキュメント

**前提条件**: plan.md (必須), spec.md (必須), research.md, data-model.md

**テスト**: 自動テストの記述要請はないため、手動検証シナリオによる動作確認タスクを含めます。

**組織化**: タスクは各ユーザーストーリーごとに整理されており、各ストーリーを独立して実装および検証することができます。

## 記述フォーマット: `[ID] [P?] [Story] Description`

- **[P]**: 並列実行可能（ファイルが異なる、または依存関係がない）
- **[Story]**: どのユーザーストーリーに属するか（例: US1, US2, US3）
- タスクの説明には、具体的なファイルパスを含めます。

## パス規約

- **単一プロジェクト**: リポジトリルートの `src/`

---

## 第1フェーズ: セットアップ (共通インフラ)

**目的**: プロジェクトの初期状態確認とビルド確認

- [x] T001 プロジェクトのルートで依存関係をインストールし、既存コードで `npm run build` が通ることを確認する

---

## 第2フェーズ: 基盤 (依存関係 of 完了)

**目的**: ユーザーストーリーの実装を開始する前に完了しておくべきコードベースの整理（設定トグルshowRibbonMenuの削除）

- [x] T002 [US3] `src/settings.ts` の `MarimoBridgeSettings` インターフェースから `showRibbonMenu` 設定項目の定義を削除する
- [x] T003 [US3] `src/settings.ts` の `DEFAULT_SETTINGS` から `showRibbonMenu: false` の初期値定義を削除する
- [x] T004 [US3] `src/settings.ts` 内の `MarimoBridgeSettingTab` から 「Enable ribbon left-click menu」設定トグルのUI構築処理を削除する

**チェックポイント**: 基盤準備完了 - 不要設定のクリーンアップが完了し、挙動の変更を開始できます。

---

## 第3フェーズ: User Story 1 - 左クリックでの直接起動 (Priority: P1) 🎯 MVP

**ゴール**: リボンアイコンを左クリックした際、メニューを挟まずに即座に marimo ホームダッシュボードが開くこと。

**単体検証**: リボンアイコンを左クリックして、直接ホームが新しいタブで開くことを確認する。

### ユーザーストーリー1 の実装

- [x] T005 [US1] `src/main.ts` 内の `addRibbonIcon` のコールバック処理を簡略化し、常に直接 `openMarimo(undefined)` を呼び出すように修正する

**チェックポイント**: この時点で、User Story 1（左クリック直接起動）が動作し検証可能な状態になります。

---

## 第4フェーズ: User Story 2 - 右クリックでのメニュー表示 (Priority: P1)

**ゴール**: リボンアイコンを右クリックした際、メニュー項目が表示されて機能すること。

**単体検証**: 右クリックした際に OS のデフォルトメニューが抑制され、 marimo のカスタムメニューが表示され、機能することを確認する。

### ユーザーストーリー2 の実装

- [x] T006 [US2] `src/main.ts` 内で `addRibbonIcon` が返す HTML要素を取得し、`registerDomEvent` を使用して `contextmenu`（右クリック）イベントハンドラーをバインドする
- [x] T007 [US2] `src/main.ts` の `contextmenu` コールバック内で `evt.preventDefault()` を呼び出し、カスタム `Menu`（Open marimo home / Create new marimo notebook）を表示する処理を実装する

**チェックポイント**: この時点で、User Story 1 と 2 が両方とも動作し検証可能な状態になります。

---

## 最終フェーズ: 仕上げと横断的関心事 (Polish)

**目的**: 品質確認と動作検証

- [x] T008 `npm run build` を実行してコンパイルエラーが無いことを確認し、さらに `npm run lint` が通ることを検証する
- [ ] T009 [P] `specs/007-ribbon-click-behavior-change/quickstart.md` の検証手順に従い、シナリオ1〜3の手動動作確認を行う
- [ ] T010 [P] 変更内容を Git にコミットする

---

## 依存関係と実行順序

### フェーズ依存関係

- **セットアップ (第1フェーズ)**: 依存関係なし - 即座に開始可能。
- **基盤 (第2フェーズ)**: セットアップ完了に依存。
- **ユーザーストーリー (第3フェーズ以降)**: 基盤フェーズ（設定の定義）の完了に依存。
  - US1 と US2 は順次（P1 → P1）実行します。
- **仕上げ (最終フェーズ)**: すべてのユーザーストーリーの実装完了に依存。

### ユーザーストーリー依存関係

- **User Story 1 (P1)**: 基盤完了後に開始可能。
- **User Story 2 (P1)**: 基盤完了後に開始可能。

---

## 実装戦略

### MVP優先 (User Story 1 のみ)

1. 第1フェーズ（セットアップ）を完了する。
2. 第2フェーズ（基盤）を完了する。
3. 第3フェーズ（User Story 1）を完了する。
4. **動作検証**: 左クリックした際に直接ホーム画面が開くことを確認する。

### インクリメンタルデリバリー

1. セットアップ + 基盤を完了（不要設定の削除）。
2. 左クリックの直接起動化（US1 / MVP）を追加し、検証。
3. 右クリックでのメニュー表示（US2）を追加し、検証。
4. 最終ビルド・テストを行いリリース準備。
